## Copy - paste local files, asynchronous

## Instructions

`node index.js`

The function `fastCopy` takes the files of the folder `static-copy` and pastes them into `static-paste`.

The folder `helpers` has some helper functions for future adaptation.
